# PepeLab Frontend — 環境變數範例
# 複製成 .env 後填入實際值：cp .env.example .env

# ── Signal API (x402) ────────────────────────────────────────────────────────
# 公開 x402 Signal API 網址。未設時程式預設使用已上線的 Vercel 部署
# （見 src/lib/pepefi/signalApi.ts 的 DEFAULT_SIGNAL_API_URL）；本機開發可改 http://localhost:4021。
# 用於 /x402 文件頁試買、Marketplace 卡片、AgentMonitor 的鏈上 /revenue。
VITE_SIGNAL_API_URL=https://agent-git-master-zuemens-projects.vercel.app

# ── Supported Networks ───────────────────────────────────────────────────────
# The frontend supports three networks automatically:
#   - Anvil Local   (chainId 31337)  — local dev
#   - Sepolia       (chainId 11155111) — core trading contracts
#   - Base Sepolia  (chainId 84532)  — AI Agent Sessions + x402
# Contract addresses are auto-resolved via src/contracts/addresses.ts.
# No per-network env var is needed; just connect MetaMask to the target network.
