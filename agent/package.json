{
  "name": "pepelab-agent",
  "private": true,
  "version": "0.1.0",
  "description": "PepeLab x402 + AI Agent — Phase 1 PoC (read-only)",
  "type": "module",
  "workspaces": [
    "shared",
    "signal-api",
    "mcp-server",
    "demo-agent"
  ],
  "scripts": {
    "signal-api": "npm run start -w signal-api",
    "mcp-server": "npm run start -w mcp-server",
    "demo-agent": "npm run start -w demo-agent",
    "build": "tsc -b --pretty",
    "typecheck": "tsc -b --pretty",
    "bundle:vercel": "npm run bundle:vercel -w signal-api",
    "bundle:check": "npm run bundle:check -w signal-api",
    "//test": "稽核 2026-08-06：agent 的測試以前沒有任何一支在 CI 跑（package.json 根本沒有 test script）。以下全部離線可跑、不需金鑰、不送任何交易。",
    "test": "npm run test:keeper && npm run test:core && npm run test:agent && npm run test:api",
    "test:keeper": "tsx keeper/core.test.ts && tsx keeper/feeds.test.ts",
    "test:core": "tsx examples/freshness.test.ts && tsx examples/edge-policy.test.ts && tsx examples/decide.test.ts",
    "test:agent": "tsx examples/audit-trail.test.ts && tsx examples/verification.test.ts && tsx examples/write-gate.test.ts && tsx examples/agent-identity.ts && tsx examples/agent-onboarding.ts",
    "test:api": "tsx examples/api-gate.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node-telegram-bot-api": "^0.64.15",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "node-telegram-bot-api": "^1.1.0"
  }
}
