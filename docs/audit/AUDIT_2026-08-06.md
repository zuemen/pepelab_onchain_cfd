> **修復狀態（2026-08-06 同日）**：本報告列出的問題已在分支 `fix/audit-2026-08-06` 全數修復，
> 驗證結果 `forge test` **653 passed / 0 failed**（稽核前 525）、前端 **93 vitest passed** + build 綠、
> agent build + test + bundle 同步檢查全綠。
>
> **三件事仍未完成，因為它們需要你本人簽送鏈上交易，不該由我代決**：
> 1. **輪替外洩的 deployer 金鑰** —— 步驟見 `docs/RUNBOOK_KEY_ROTATION.md`，
>    腳本見 `contracts/script/RotateOwnership.s.sol`（先 `DRY_RUN=true`）。
>    鏈上實測影響 **9 個合約**，比原先估計的 4 個多，含 x402 真實收入的 FeeRouter。
> 2. **重部署 exchange** 以解掉線上仍是 300 秒的 `FUNDING_INTERVAL` ——
>    `contracts/script/RedeployExchange.s.sol`。**dry-run 顯示線上尚有 48 個未平倉部位、
>    7,820 USDC 保證金**，切換 `InsuranceVault.setExchange` 會讓舊交易所失去償付後盾，
>    必須先讓這 48 個部位歸零。
> 3. **作廢 Infura project `7cdfb492…`** —— 已從 HEAD 移除，但 git 歷史仍可達，
>    刪檔不等於失效。
>
> 合約層的修復**只存在於原始碼**；已部署的 bytecode 要等重部署才會生效。

# PepeLab On-Chain CFD 全面稽核報告

掃描日期：2026-08-06
掃描標的：`github.com/zuemen/pepelab_onchain_cfd`，本機分支 `fix/price-feed-liveness`（= PR #6，領先 master 14 commits，未合併）
方法：五個平行稽核（核心合約 / 週邊合約與預言機 / agent 後端與 x402 / 前端 / 維運 CI 機密文件），加上對 Base Sepolia 的鏈上實測。
凡標「PoC ✅」者為實際寫測試跑出的數字；凡標「鏈上實測」者為對 `https://sepolia.base.org` 的真實查詢。

---

## 摘要：必須今天處理的四件事

| # | 問題 | 一句話 |
|---|---|---|
| P0-1 | **Deployer 私鑰在 public repo 的 git 歷史裡，且仍是全部合約的 owner** | 任何人可 mint 無限 mUSDC、改所有風控參數、提走 1.29 ETH |
| P0-2 | **線上 `FUNDING_INTERVAL` 是 5 分鐘（216%/日），不是 source 裡的 8 小時** | position #0 已欠 $1,827 funding（本金 $50） |
| P0-3 | **排程 keeper 每次全部寫入失敗，卻回報成功** | 喂價目前只靠人工 dispatch 撐著 |
| P0-4 | **PepeAMM 不是 AMM，是零滑點預言機定價** | 每次 oracle 跳動都可無風險套走池子 9%+ |

---

# 一、機密外洩

### S1 · Critical — Deployer／owner 私鑰公開可取得

- **憑證**：`0x2b94ce61…a0ec0d`（完整值在 git 歷史中，此處截斷）
- **出現於**：`frontend/.env.example`、`frontend/price_keeper.cjs`，commits `1d8536e`(2026-05-28)、`e912bff`(2026-06-16)、`cf712ec`(2026-06-19)
- **現況**：已從 HEAD 與 master 的工作樹刪除，但三個 commit 從所有遠端分支可達；`gh repo view` 確認 repo 為 **PUBLIC**
- **鏈上實測（我親自驗證）**：
  - `cast wallet address` → `0xE80A81360608C1342e66743F70a00f75d792Eb93`
  - 該地址目前是 `PerpetualExchange 0xEf75…c072`、`MockUSDC 0x69fd…b035`、`PepeToken 0xccd0…6a0f`、`InsuranceVault 0xB364…D812` 的 **owner**
  - 餘額：Base Sepolia **1.2933 ETH**、Sepolia 3.647 ETH、官方 USDC 59.98、MockUSDC 33,217
  - 同時是 x402 的 `payTo` treasury、以及 session #0／#6 的 `user`（即 agent 下單的授權來源）
- **可做的事**：無限 `mint` mUSDC 灌爆保證金池、`setTradingFeeBps(100000)` 吃掉所有人保證金、`setMaxPriceAge` 關閉 staleness、改 FeeRouter 收款、提走全部 ETH，以及冒用 treasury 身分收 x402 款項。
- **處置**（依序）：
  1. 產生新 deployer → 對四個合約 `transferOwnership` → 移轉全部餘額
  2. 把 `0xE80A8136…` 永久視為已公開地址（`agent/.env.example` 的 `PAY_TO` 預填值一併換掉）
  3. 確認 GitHub secret `KEEPER_PRIVATE_KEY` 目前值（鏈上看 CI 用的是 `0x540aECD3…ef17`，與外洩值不同）從未等於外洩值
  4. filter-repo 改寫歷史可做，但**輪替才是止血**，改寫無法撤回已被抓取的內容

### S2 · High — Infura project id 仍在 HEAD 的被追蹤檔案裡

`frontend/check_all_pepe_balances.cjs:3` 硬編碼 `7cdfb492…e9a4d`，master 另有 `frontend/price_keeper.cjs:49`。
`docs/RUNBOOK_KEEPER.md:183` 寫「雖已刪檔，仍留在 git 歷史中」——低估了：它連 HEAD 都還在。

### 掃描乾淨的部分
助記詞、Telegram token、Etherscan/BaseScan key、AWS/GitHub token 在工作區與全歷史皆未發現。`agent/.env`、`agent/tg-bot/vc.json`、`agent/audit/` 從未被 commit，gitignore 逐項驗證有效。Anvil 內建測試金鑰（`0xac0974be…`）為公開帳號，無須處理。

---

# 二、核心永續合約（`PerpetualExchange` 及其週邊）

## Critical

**C-1 · mark price 溢價可在同一區塊內無風險套利** — `PerpetualExchange.sol:918, 797, 883-887`
進場價用 index、PnL 用 mark；`_closePosition` 在 `globalLongNotional -= notional` **之前**就先算 pnl，所以平倉時自己的部位仍在推高對自己有利的 mark。
PoC ✅：`setMarkPremiumCapBps(100)` 後，margin 100,000 / 5x 開→平，freeMargin 淨增 **+4,000e18**，零價格風險（1% 溢價 > 0.2% 來回手續費）。
目前 `markPremiumCapBps = 0`（鏈上實測）故休眠，但這代表這個功能**永遠不能開**。測試無覆蓋。

**C-2 · 虧損封頂、獲利無上限，且 `closePosition` 完全沒有壞帳處理** — `:859-865, 892-894, 478`
`closeAmount < 0` 夾成 0，輸家損失封頂於 margin；贏家 PnL 全額入帳。ADL／保險金墊付**只寫在 `liquidatePosition`**，而 `_closePosition` 這條路不但沒有 ADL，還在 `:892` 從保險金庫再送 10% margin 給破產的輸家。使用者可主動平掉破產倉，完全繞開清算。
PoC ✅：兩地址對沖各 100,000 / 5x，價格 +50% → 合計 claims 2,000,000 → 2,150,000，池子淨補貼 **+150,000 USDC**。

**C-3 · portfolio margin 下可用無界 `userPositions` 做 DoS** — `:718-732, 825`
`userPositions[owner]` 只增不減，`_accountState` 每次走完整條陣列。開平倉 margin 全額退還，灌陣列近乎零成本。
PoC ✅：`getAccountHealth` 8,947 gas → 2,000 筆死條目時 **1,194,968 gas**（每條 593 gas），約 50,590 條即超過 30M 區塊上限；`liquidatePosition` 在 2,000 筆下 1,260,410 gas。
`portfolioMarginEnabled` 開啟時，攻擊者灌到約 5 萬筆就永遠清不掉。鏈上實測目前為 `false`。

## High

**H-1 · ADL 掃描視窗被已關倉部位耗盡，backstop 靜默失效** — `:525-529, 826`
`_autoDeleverage` 從 index 0 掃，上限 `MAX_ADL_SCAN=128`，`assetPositionIds` 同樣只 push 不壓縮。前 128 個若都是關倉條目，迴圈全 `continue`，**不 revert、不發事件**。
PoC ✅：130 筆開→平灌滿索引後，1,000 margin 5x 多單 −25% 破產清算 → 對手空單（PnL +1,250）完全沒被 haircut。
**鏈上實測 `adlEnabled = true`、`InsuranceVault` 餘額 = 0** → 目前線上這道 backstop 既沒有資金、又可被灌爆。

**H-2 · funding 積欠以 `intervals` 線性放大且無上限** — `:590-593, 615`
PoC ✅：多單 100,000 margin / 500,000 notional，閒置 200 天後一次 `settleFunding` → `pendingFunding = 2,190,000e18`，等於 notional 的 438%、margin 的 21.9 倍。付款方付不出來（見 C-2），差額即壞帳。

**H-3 · funding 接收側單位費率被 `payerOI/receiverOI` 放大到無界** — `:622, 626`
PoC ✅：多方 1,000,000 notional、空方僅 10 USDC，warp 8h 後該 10 USDC 部位一次收到 **7,400e18** USDC（自身 margin 的 740 倍）。總額守恆，但薄側的應收由池子先付。

**H-4 · InsuranceVault 抽乾後下一位存款人被稀釋** — `InsuranceVault.sol:73`
`if (supply == 0 || totalAssets == 0) return usdcAmount;` — bailout 可把 `totalAssets` 打到 0 而不燒 share。
PoC ✅：alice 存 1,000 → 抽乾 → bob 存 1,000 得 1,000 shares → 立刻只值 500e18，alice 的廢 shares 被復活成 500e18。

**H-5 · `CopyTracker.unfollowAndCloseAll` 一旦有部位被清算就永久磚化** — `CopyTracker.sol:170-172`
迴圈內 `closePositionFor` 無 try/catch，任一部位已清算即 revert `PositionAlreadyClosed` → `rec.active` 永遠 true、slash 永遠不觸發、follower 無法解除跟單。PoC ✅。

**H-6 · 任一 `authorizedAgents` 可強制平掉任何人的任何部位** — `:403-406`
`closePositionFor(address owner, uint256 id)` 的 `owner` 由呼叫者任意指定，`_closePosition` 只比對這個參數，交易所層等於沒有授權。`AgentSessionManager` 自己有比對 `pos.owner == s.user`，但那是上層自律。PoC ✅。
**鏈上實測目前有三個 authorizedAgents**：新 SessionManager `0x4E7cC1B7…`、**舊 SessionManager `0x5Ebcc64C…`（從未撤權）**、CopyTracker `0x9635714…`。

## Medium

- **M-1** `_routeVaultFee` 在手續費根本沒收到時仍從儲備付款（`:501, 903, 746-755`）。PoC ✅：`setVaultFeeShareBps(10000)` 下，closeAmount=0 的平倉讓 exchange 少 5e18、vault 多 5e18，全出自池子。
- **M-2** 清算沒收 100% 殘值含維持保證金緩衝（`:466-477`）。PoC ✅：1,000 margin 5x −15.1% 後殘值 245 USDC，owner 拿 0。
- **M-3** setter 缺上下界與事件：`setTradingFeeBps`／`setBorrowFeePerHour` **完全無上限**（可設 100000 bps 吞掉全部保證金）、`setMaintenanceMarginFor` 允許 10000 bps、`setMaxPriceAge` 只擋 0；`setFeeRouter`／`setInsuranceVault`／`setExecutionFee`／`setCopyTracker` 皆無事件、無零地址檢查。
- **M-4** 全程未用 SafeERC20、`approve` 未歸零（`:352, 360, 368, 472, 897, 475, 752`）。只有 `v2/AssetVault*` 有用。repo 內同時存在 `MockUSDT.sol`。
- **M-5** `AssetVaultV2*.registerAsset` 可在 outstanding > 0 時改指到另一個 token（`v2/AssetVaultV2.sol:139-149`），`unregisterAsset` 有保護而 `registerAsset` 沒有。PoC ✅：改綁後 redeem 必 revert，負債仍記著。
- **M-6** `AssetVaultV2` / `V2_1` 的 redeem 會記入沒有背書的 `accruedFees`（守衛弱了一個 `feePaid`）；**`V2_2` 已修**，但 V2.0／V2.1 兩份仍留在 `src/`。
- **M-7** `outstandingValue` 跳過無法定價的資產，讓 mint gate 樂觀通過；`V2_1` 的 `ratioIsStale()` 只給前端看，`mint` 本身沒用它。
- **M-8** `AgentSessionManager.closePositionForSession` 不限於本 session 開的倉（`:212-222`），agent 可平掉使用者在 session 外自行開的任何部位；`_createSession` 未驗 `maxLeverage != 0`（會造成永遠 revert 的死 session）。
- **M-9** `AssetVault`（V1）無 staleness、無 ReentrancyGuard、非足額抵押——**這項已有測試明確覆蓋**（`AssetVaultSolvency.t.sol`），屬已知並記錄的設計缺陷。

## Low
`FeeRouter.receivePerformanceFee` 只記帳不自己收款（惡意 copyTracker 可把別人的 USDC 記成自己的 earnings）；`CopyTracker` 的 ETH 餘數永久卡住（無提領函式）；slash 基準含手續費天生偏虧損；`MIN_MARGIN=10e18` 與 `rawPrice*1e10` 硬編碼「USDC 18 位」而 `usdc` 是 immutable；`_freshPrice` 不檢查 `rawPrice > 0`；執行費溢付不退還；`TraderStake.executeUnstake` 在期間被 slash 會 underflow；`getPositionValue` 不扣 funding 與費用（前端顯示偏高）；ADL 殘餘壞帳靜默不發事件。

## 已確認是刻意設計、非缺陷
oracle 過期 → 開／平／清算全部 revert（fail-closed，`SecurityFixes.t.sol` 有覆蓋）；`spentMargin` 不因平倉退還（終身額度，無循環開倉漏洞）；funding 雙索引守恆模型數學正確。

---

# 三、週邊合約 / 預言機 / 部署腳本

## Critical

**PA-1 · PepeAMM 根本不是恆定乘積 AMM** — `PepeAMM.sol:42-80`
輸出僅為 `amountIn × oraclePrice`，儲備完全不進入定價，`x*y=k` 不存在 → 零滑點。
PoC ✅：線上池 0.1 ETH / 300 mUSDC，攻擊者以 300 mUSDC 換出 0.0997 ETH → oracle +10%（日內常見）→ 換回 328 mUSDC，**淨賺 28.02 mUSDC = 池中 USDC 的 9.3%，每次 oracle tick 可重複**。因為沒有滑點，套利者不會把價格推回，池子單向流失直到歸零。
測試方向相反：`PepeAMM.t.sol:51-70` 的斷言 `assertEq(amm.getPrice(), priceBefore)` 把這個漏洞當規格寫死了。

**PA-2 · PepeAMM 完全丟棄 oracle 的 `updatedAt`** — `:43, 62, 84, 94, 101`
只檢查 `price8 > 0`。全系統唯一沒有 staleness 檢查的路徑。keeper 一停，任何人都能用陳舊價格配合 PA-1 套乾池子。

**PA-3 · `MockUSDC.mint` 無任何權限** — `MockUSDC.sol:45-47`（`MockUSDT` 同）
PoC ✅：router 內 10 ETH → `usdc.mint(self, 30_000e18)` → `swapUSDCForETH` → **零成本取走全部 10 ETH**。`setSwapRouter` 那段 owner-only 的「防搶跑」註解因此失效：burn 守得再嚴，供給端是開放的。

## High

- **PA-4** `PepeIncentives.claimTierReward` 不對 `positionIds` 去重（`:149-153`）。PoC ✅：單一 1,000 notional 部位傳入 `[1]×1000` → 直接領走 Diamond 等級 50,000 PEPE。
- **PA-5** PepeAMM 沒有 `removeLiquidity`，注入的流動性**永久鎖死**，連 owner 都拿不回。
- **PA-6** `DeployWithPyth.s.sol:61-68` 從未呼叫 `setFeed` → Chainlink 源對每個資產都 revert `FeedNotSet` → 號稱的雙源聚合器**實際是單源 Pyth**，`maxDeviationBps` 純裝飾。
- **PA-7** `AggregatorOracleAdapter` 單源退化是 fail-open（`:75-76`），且會讓 GuardedOracle 的**緊急凍結被悄悄繞過**（`_probe` 的 catch 把「凍結」誤讀成「來源掛掉」）。現有測試把 fail-open 寫成期望行為。
- **PA-8** `RedeployExchange.s.sol`（未 commit）三處接線遺漏：漏 `traderStake.setCopyTracker`（新 CopyTracker 永遠無法罰沒）、漏 `feeRouter.setCopyTracker`（跟單績效費全 revert）、`INS_VAULT.setExchange(新)` 會**弄壞舊交易所**（bailout 硬性要求 `msg.sender == exchange`）→ docstring 宣稱「舊部位仍可平倉、nothing is destroyed」與行為正面矛盾。
- **PA-9** `PepeStaking` 本金與獎勵同池，且缺 Synthetix 的 `rewardRate <= balance / duration` 檢查 → 可超發，先領的人吃掉後面質押者的本金。

## Medium
Aggregator 偏離死鎖（雙源差 >1% 即 revert，**連清算一起擋住**，而行情劇烈時 1% 差距是常態）；GuardedOracle 的 deviation cap 與 reference 檢查互斥，大幅跳空時兩道閘鎖死任何價格都貼不進去；PythOracleAdapter 完全不使用 confidence interval 且用 `getPriceUnsafe`；ChainlinkOracleAdapter 缺 round 完整性檢查（`answeredInRound`／`ts != 0`）；三個 adapter 的 `STALE_THRESHOLD = 24h` 對永續合約過鬆（Chainlink heartbeat 約 1h）；ESG 持有獎勵不檢查 `pos.isOpen`（開倉後秒平，30 天後照領）；`EsgRewardDistributor` 連持有期都沒有；`KYCRegistry.submitKYC` 自助即時通過 → PepeClaim 女巫零成本，而同一個 registry 就是 RWA 合規閘；faucet cooldown 是 per-address，PoC ✅ 合約在 constructor 內領取，單筆交易 50 次共 50,000 mUSDC；`DeployGuardedStack.s.sol` 三個角色同一把鑰匙，GuardedOracle 的整套風險論述回到原點。

---

# 四、agent 後端 / x402 / keeper

## Critical

**A-1 · `decide()` 把 API 錯誤回應當成「做空訊號」→ 真實下單** — `examples/x402-autonomous.ts:115, 73-76`
`const data = body?.data ?? body`：錯誤 body 沒有 `data`，於是 `data` 變成錯誤物件本身。`decide()` 只檢查 `!d || d.isStale`，兩者皆假；`recommendation` 為 `undefined` → 不等於 `"no_trade"` → `isLong = false` → **送出真實 SHORT 單**。
實測：503 `price_stale` body 與 402 未付款 body 都得到 `{"action":"short","margin":50,"leverage":3}`。
諷刺的是 `app.ts:306-340` 的 stale 閘門正是本分支新加的——它一回 503，agent 立刻做空。
（`x402-loop.ts:82` 用 `?.data` → `!d` → skip，**不受影響**，只有 autonomous 中招。）

**A-2 · Telegram bot 預設對全世界開放下單** — `tg-bot/index.ts:6, 43`
`if (ALLOWED && chatId !== ALLOWED) return;` — `TELEGRAM_ALLOWED_CHAT` 未設時（`.env.example:85` 明寫「留空＝不限制」），任何知道 bot 名稱的人傳「做多 sBTC 5x 保證金 1000」就能用 session key 上鏈開真倉，直到預算用光；`/pos` 也對任何人吐 session 內容。

## High

- **A-3** `openPositionForSession` 的 VC 閘門是**可選的**（`write.ts:183`），而三個呼叫端都沒帶：`x402-autotrade.ts:114` 完全沒有、`tg-bot/index.ts:11,59` 未設路徑時靜默跳過、MCP `authVcJson` 標 `.optional()`。應該是「無 VC 即拒絕」（如 `x402-autonomous.ts:158` 所做）而非「無 VC 即免驗」。
- **A-4** `closePositionForSession` 完全無授權層（`write.ts:260-284`）：沒有 VC、沒有風險閘、沒有任何檢查，可平掉使用者任意部位、實現虧損。與開倉的 VC 論述完全不對稱。
- **A-5** keeper 寫 MockOracle **完全沒有偏離上限**（`keeper/run.ts:184-194`）：只要偏離 ≥0.1% 就寫。GuardedOracle 那條有 `stepTowards` 保護，而 `PerpetualExchange` 讀的正是沒保護的 MockOracle。Yahoo 在拆股日／換合約回一個「數值合法但離譜」的價格，下一輪就會依此清算所有部位——這正是 `core.ts:3-7` 註解宣稱要防的事故，卻只擋住了「HTML 混進來」。
- **A-6** `enrichOracle` 的 `recommendation` 仍用 24 小時的 `isStale`，不是交易所的 `maxPriceAge`（`aggregate.ts:136`）。`tradableNow` 已經算好了卻沒被 `computeEdge` 使用。修的只有**販售**閘門，**決策**欄位還是錯的。

## Medium
ERC-8126 attestation 的 `riskTier`／`evidence`／`passed`／`name` 不在摘要內，**竄改後驗證仍通過**（實測：改 tier 為 low → `valid:true`），註解宣稱的「任何竄改都會改變 digest」不成立；attestation 無有效期無 nonce 可無限重放；verifier 私鑰未設時每個 Vercel 實例一把隨機金鑰且不標示降級；WV 檢查未出示持有證明時直接從分母移除 → 任何 DID 都拿同一組低分；`/agent/:did/verification` 外呼無 timeout 且目標 URL 由 Host header 決定（未認證的 SSRF-ish 放大器）；x402-loop 的 session 額度只在啟動時讀一次永不刷新、VC 也只驗一次；x402-loop **先付錢再判斷要不要跳過**（192 次付費/日且無累計上限，`for(;;)` 無限重試）；Vercel 多實例下 nonce 序列化只在單一實例內有效；`X402_SETTLEMENT_TOKEN` 在 `app.ts` 與 `settlement.ts` 有**兩個不同的預設值**（一個官方 USDC、一個 MockUSDC），`_assertCurrencyMatch` 抓不到；**`/oracle` 完全沒有結算**（自主 agent 打的就是它，那筆錢從未進 FeeRouter，而 `GET /` 宣稱走 70/20/10）；付款發生在輸入驗證之前（`/oracle/sDOGE` 付了錢才回 400，無退費）；`buy-signal.ts` 未設 TRADER 時對零地址付費並把 70% 分潤送往零地址；`audit-verify.ts` 有假陽性（positionId 解析失敗會被當成 skip 但仍印 ✅、`settlementTx` 從未被驗證、只檢查 `position.owner === session.user` 故可抄別人的 positionId、`vcId` 只綁簽章位元組不綁紀錄內容，且 JSONL 是 agent 可任意重寫的本地檔無 hash chain——「不可否認」的說法過強）；keeper GuardedOracle 鏡射失敗完全靜默；10/11 資產跳過不報警；兩支 keeper workflow 同 `*/15` 且無 concurrency，共用同一把金鑰會撞 nonce；**agent 的測試沒有任何一支在 CI 跑**（`package.json` 無 test script）；Vercel 服務的是 commit 進 repo 的 4.8MB bundle，**沒有 build step**，改了 `src/*.ts` 忘記重打包線上就是舊碼。

## Low
VC 的 DID chainId 未綁定（實測改成 `eip155:1` 仍 `valid:true`，且 `vcId` 不變）；`checkSCV` 把 API key 放在 URL query 且錯誤訊息會進公開回應；`/demo/buy-signal` 是未認證的 RPC 放大器（per-IP 冷卻讀可偽造的 `x-forwarded-for`，`lastBuyByIp` Map 永不淘汰）；tg-bot 對槓桿／保證金無上界且 `sendMessage` 未 await 未 catch；Yahoo 報價未檢查幣別與報價時間（週末回上一個收盤價，keeper 每 15 分鐘照寫 → **oracle 的 updatedAt 一直新鮮但價格是兩天前的**，對「price feed liveness」這個分支主題是實質的偽新鮮度）；CORS 全開且免費端點無節流；`settlement.ts` 對任意 token 無條件先試 `mint`。

## 實測確認不成立的疑慮
簽章 malleability（ethers 6.16 的 `Signature.from` 擋下 non-canonical s）；結算 decimals 換算（動態讀 `decimals()`，6-dec 與 18-dec 皆正確）；`.env.example` 無真實私鑰；`candles.ts` 與 `assetIdOf` 無注入面。

---

# 五、前端

`yarn build` 綠、`yarn vitest` 綠——但**全前端只有 1 個測試檔 8 個 test**（436 個原始檔），任何「前端測試全綠」都應理解為覆蓋率接近零。

## High
- **F-1** `ExchangePage.tsx:500-519, 1317-1343` — **主要交易頁完全沒有 stale 擋單**（我實測 grep 計數為 0）。Terminal 有擋，`/exchange` 沒有。oracle 過期時頁面仍顯示 CoinGecko 綠色即時價，使用者按 Open Long → 簽名付 gas → 鏈上 `StalePrice` revert。這正是本分支的核心功能，漏接了最大的下單路徑。
- **F-2** `CopyPage.tsx:222-239` — 跟單開倉同樣無 stale 擋單，且會乘 N 筆 execution fee 一起打水漂。
- **F-3** `WhaleTrackerPage.tsx:35, 239-240` — `DEPLOY_BLOCK = 10_874_200` 是 **Ethereum Sepolia** 的區塊。在 Base Sepolia（2 秒/塊、現高數千萬）上等於數千次串行 `eth_getLogs` → 頁面卡數分鐘、公共 RPC 限流。`FEATURED_WHALES` 還全是 Anvil 助記詞位址。

## Medium
`DashboardPage.tsx:538` 的「把 PEPE 加入錢包」**寫死 Sepolia 的 PepeToken 位址**（我已核對：`0xa364F4…` 是 addresses.ts 的 SEPOLIA 區塊，Base 應為 `0xccd05c…`）→ Base 使用者加到錯誤合約餘額永遠 0；平倉路徑未被 stale 擋（與 `TerminalView.tsx:61-62` 自己的註解矛盾）；`useWhaleAlerts.ts` 單發 50,000 塊 `queryFilter` 且 `AVG_BLOCK_TIME=12` 在 Base 上高估 6 倍；帳戶選單「My Trader Profile」連到寫死的**非法地址**（24 個 hex 字元）；mock 錢包位址是 21 bytes（42 個 hex 字元，合法應為 40）；`TraderStakePage` 的「收割」直接呼叫 `onlyOwner` 的 `pepeToken.mint()`（對一般使用者 100% revert，錯誤文案自己承認要 owner 錢包）且 `setInterval(…,100)` 每 100ms 重繪整頁；`useESG.ts` 無 0x0 守衛 → Base 上 ESGRegistry=0x0 仍串行發 11 次呼叫，Exchange 頁**永遠顯示「ESG 資料載入中…」**；RPC 輪詢風暴（兩組 8 秒輪詢 + `useLivePrices` 每 8 秒對 11 個資產串行讀取，且是全站唯一沒有 timeout/safeRead 的鏈讀迴圈）；同一頁 USDT／mUSDC 標籤互相矛盾，而 `tokenLabel.ts` 的 `STABLE_LABEL` **無任何檔案引用**（死碼），各頁自行手寫才會岔開；`AgentMonitorPage.tsx:184, 253` 未驗 API shape 可整頁 crash（同功能的 `HeroKpiStrip.tsx:113` 有守衛，標準不一）；`useKYC.ts` 讀取失敗 **fail-open**（預設與錯誤時皆 `isVerified=true`）——合規閘應 fail-closed。

## Low
`routes/sections/dashboard.tsx` 從未掛載（死路由，但 `pages/dashboard/*` 仍被打包，如 90kB 的 jwt-sign-up-view）；terminal 系 6 個檔 `type Contracts = any`；execution fee 在 UI 寫死 0.001 ETH 而送單讀鏈上值；清算價估算兩頁公式不一致（一個含 5.1% buffer 一個沒有）；`ExchangePage.tsx:574` 除法無 `entryPrice > 0` 守衛（同檔其他兩處都有）；`AdminTreasuryPage` owner 判定用寫死 EOA 而非鏈上 `platformTreasury()`（其實已經抓回來卻不用）；`AdminOraclePage` 每秒全頁重繪；`prettyError` fallback 把 raw revert 原文吐給使用者；多處序列 O(N) 鏈讀。

## 正面確認
0x0 守衛在絕大多數頁面已到位；**decimals 使用全面正確**（18/8/6 dec 無一處用錯）；approve 全為精確額度無 MaxUint256；AMM swap 有 0.5% minOut；interval/listener 均有 cleanup；x402 文案正確維持「官方 USDC」；無 PepeFi 品牌殘留；ESG 留在 11155111 是刻意設計。

---

# 六、維運 / CI / 文件真實性

## Critical
**O-1 · 排程 keeper 每次全部寫入失敗卻回報成功** — `.github/workflows/base-sepolia-keeper.yml`（master 版）
每個 `cast send` 掛 `|| echo "…skip"`，寫 0 筆仍 success。
實證（我親自查 run log）：run `31069004586`（今天 03:38）**10/10 資產 `updatePrice failed … skip`、settleFunding 全部 `gas required exceeds allowance (0)`，job 綠**。歷次排程 run 時長 14–33 秒（真正有寫入的手動 dispatch 是 1m35s）。
→ 線上喂價目前**只靠人工 dispatch 撐著**。修好的版本只存在於未合併的 PR #6。

## High
- **O-2** `oracle-health.yml` **不存在於 master** → 宣稱「每 3 小時獨立驗鏈上事實」的補償控制完全沒在跑，O-1 的靜默失敗沒有第二道網。
- **O-3** `contracts-ci.yml` / `frontend-ci.yml`（master 版）的 `push: branches: [main]`，但預設分支是 **master** → push 觸發從未生效，只有 PR 會跑。
- **O-4** `admin-base-sepolia.yml:62-68` 把 `${{ github.event.inputs.function }}` / `args` 直接內插進 `run:` 且 `args` 未加引號 → **指令注入**，可在持有 `KEEPER_PRIVATE_KEY` 的 job 中執行任意指令並外送金鑰。
- **O-5** **PR #6 仍 OPEN**。GitHub 排程只讀預設分支 → keeper 修復、CI 分支修正、oracle-health 三者一行都沒在生產跑。
- **O-6** `contracts/script/RedeployExchange.s.sol` **未 commit**：唯一能修掉線上 216%/日 funding 的東西只存在於這台機器。
- **O-7 依賴漏洞**：frontend `npm audit --package-lock-only` 16 個（13 high）——`vite 7.1.7`、`rollup ≤4.58`、**`react-router`（CSRF／open redirect／儲存型 XSS／DoS，影響線上而非只有 dev）**、`yaml`；agent 36 個（7 high），集中在 `@reown/appkit*` → `@walletconnect/universal-provider`。
- **O-8 SessionManager 位址三方矛盾**：`README.md:44`、`docs/CAPSTONE_DELIVERABLES.md` 與根目錄 `create-session.sh:21` 寫舊的 `0x5Ebcc64C…`（**無** per-session 資產白名單），而前端／agent／範例寫新的 `0x4E7cC1B7…`。照文件操作會在沒有資產閘門的舊 manager 上建 session。

## Medium
六條 workflow **全部沒有 `permissions:`**（繼承 repo 預設，常為 write-all）、**全部沒有 `concurrency:` 與 `timeout-minutes:`**（今天 04:40 的 dispatch 就與排程重疊過）、第三方 action 只 pin tag 未 pin SHA；新版 keeper 仍把所有失敗一律說成「funding interval 未到」；`keeper/run.ts:201-212` 只有全部失敗才 exit 1，部分失敗只出 warning；`oracle-health.yml` 的 `HEALTH_MAX_AGE` 等於巡檢間隔（單次延遲即誤報）；keeper 用 `npm install` 而非 `npm ci`；`contracts/.gitignore` 用 `!/broadcast` 反解根目錄規則 → 36 個 broadcast 檔實際被 track（內容無私鑰，但兩份規則語意矛盾）；`frontend/` 同時 track `yarn.lock` 與 `package-lock.json`（後者永遠不會被驗證的第二真相來源）。

## 文件真實性
- **High** — `README.md:50`「Funding rate — 8h interval」、`CAPSTONE_DELIVERABLES.md:129`「先前 5min…已修正」。**鏈上實測 `FUNDING_INTERVAL() = 300`**。source 確實已改成 `8 hours`（`PerpetualExchange.sol:53`）但它是 `constant`，只有重部署才會上鏈。
- **Medium** — 測試數字三份文件互不相同且全部過期：ADR-001 寫 314、VERIFICATION_REPORT 寫 318、CAPSTONE 寫 320；CI 實測 **525 passed / 0 failed（57 suites）**。
- **Medium** — `VERIFICATION_REPORT.md` 的 keeper 一列寫「⏳ 待 secrets」，實際 secrets 早已設定，真正的狀態是「在靜默失敗」；第 6 項寫 `npm run build` 但專案是 yarn-only。
- **Medium** — `RUNBOOK_KEEPER.md:29-36` 與 `admin-base-sepolia.yml:6-16` 都宣稱「keeper 餘額 0、MockOracle.owner 仍是 deployer」。**鏈上實測 owner 已是 `0x540aECD3…ef17`、餘額 0.0499 ETH**，Step A/B 已被執行過，文件會誤導下一個維運者。
- **Medium** — `README.md:9` 說 Sepolia 已停更凍結，但 `price-keeper.yml` 每 15 分鐘仍在跑它；`README.md:54-60` 描述的是 master 舊版 keeper（CoinGecko/Stooq + ±45% guard），本分支已改成 Yahoo + Pyth relay，而 Stooq 已死（CI log 實證回傳 HTML 404）。
- **Low** — `SECURITY_NOTES.md` 開頭「code review 結論為無可被利用的嚴重漏洞」與本報告不符。

---

# 七、鏈上實況快照（2026-08-06，Base Sepolia 84532）

| 項目 | 實測值 | 備註 |
|---|---|---|
| `PerpetualExchange` | `0xEf75ECA6…c072` | owner = 外洩金鑰地址 |
| `FUNDING_INTERVAL` | **300**（5 分鐘） | source 是 8h → **bytecode 與 source 分歧** |
| `MAX_FUNDING_RATE_BPS` | 75 | 合計上限 ≈216%/日 |
| `pendingFunding(#0)` | **1,826.96 USDC** | 該倉 margin 僅 50、notional 100 → 欠款是 notional 的 1,827% |
| `cumulativeFundingIndex(sBTC)` | 18.27e18 | 從 index 0 持有至今的多單，每 1 單位 notional 欠 18.27 |
| `getPositionValue(#0)` | 79.22 | 平倉時 funding 會把它整個吃掉並產生壞帳 |
| `globalLong / globalShort (sBTC)` | 1950e18 / 1950e18 | 目前均衡，`getFundingRate = 0`（新倉暫不累積） |
| `adlEnabled` | **true** | 但 backstop 資金為 0 |
| `InsuranceVault` 餘額 | **0 mUSDC** | ADL 觸發時直接 haircut 贏家 |
| `portfolioMarginEnabled` / `markPremiumCapBps` / `vaultFeeShareBps` | false / 0 / 0 | C-1、C-3 目前休眠 |
| `maxPriceAge` | 21600（6h） | 目前價齡 51 分，11 資產全 fresh |
| Oracle 價格 vs CoinGecko | sBTC 64,612 vs 64,838（0.35%） | 喂價本身準確 |
| `authorizedAgents` | 新 SM `0x4E7cC1B7…`、**舊 SM `0x5Ebcc64C…`**、CopyTracker | 舊 manager 從未撤權 |
| 舊 SM session #6 | **仍未撤銷、2027 到期** | 舊 manager 無資產白名單，等於資產閘門可繞過 |
| 新 SM session #0 | user = agent = `0xE80A8136…` | **授權對象就是 owner 自己的主錢包**，session key 隔離的敘事在 live demo 上並未成立 |
| `nextPositionId` | 49 | |
| 線上 x402 API | `/` 200、`/oracle/sBTC` 402、`/revenue` 200 | 服務健康 |
| 402 challenge 的 `resource` | **`http://`**（非 https） | 分支已修，master 仍是 http |
| `/revenue` | `count: 0` 但 `feeUsd: 0.04` | `onchainRevenue.ts:61` 硬編 count |

---

# 八、建議處置順序

1. **輪替 deployer 金鑰**（S1）——在此之前其他修復都沒有意義，因為 owner 權限是公開的。
2. **合併 PR #6**（O-1／O-2／O-3／O-5）——讓 keeper 修復、獨立健檢、CI 觸發真的在生產跑。
3. **commit `RedeployExchange.s.sol` 並修掉 PA-8 的三處接線**，然後重部署 exchange 解掉 5 分鐘 funding（P0-2）。
4. **PepeAMM 下架或改寫**（PA-1／PA-2／PA-5）——現況等於一個對外開放的免費提款機。
5. **`MockUSDC.mint` 加 `onlyOwner`**（PA-3）、faucet 加 `tx.origin` 檢查（PA-9 的 M9）。
6. **修 A-1**（`decide()` 要先檢查 `res.ok` 與 `recommendation` 白名單）與 **A-2**（tg-bot 白名單改為必填），這兩個會直接花掉真錢。
7. **前端 F-1／F-2** 把 stale 擋單接到 `/exchange` 與 `/copy`。
8. 其餘依嚴重度排入。

不建議在解決 1–3 之前對外做任何 live demo。
