# x402 Signal API 成本、結算與容量說明（Cost & Limits）實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 x402 付費層的成本結構、結算順序、容量上限寫成可回推到程式碼與鏈上實測的文件，並修掉對外文案與程式碼行為不一致的地方。

**Architecture:** 以文件為主（新增 `docs/COST_MODEL.md`，更新 `KNOWN_LIMITATIONS.md` / `NEXT_STEPS.md`），程式碼只做字串、註解與一個既有腳本內的新 MODE 分支。第 3、6、7 點的原始問題已被 2026-09-17 的 ledger + worker 重構修掉，本輪只記錄現況，不硬加欄位。

**Tech Stack:** TypeScript (tsx)、Hono + x402-hono 0.5.3、ethers v6、React + MUI（frontend，yarn）、Base Sepolia。

**Spec:** 使用者在對話中給的 8 點任務（2026-09-23）＋ 決議：「第 3、6、7 點只記錄現況，不硬改」「其餘照原題寫完整計畫」。

## Global Constraints

- 不新增新的具名方法／函式；只在既有結構內調整，或寫文件。（`Promise.all` 內的匿名 callback 視為既有結構內的寫法。）
- 每個宣稱都要能回推到 repo 內的程式碼或鏈上實測；估算必須寫出計算式與假設。
- 對外文案（frontend locales、README、docs 頁）與程式碼行為必須一致。
- 敏感值（私鑰、RPC URL／key）一律不出現在任何輸出、文件、commit。
- 不改定價（`PRICE_SIGNALS = 0.01`、`PRICE_ORACLE = 0.005`，app.ts:52-53）。
- 文件語言跟隨目標檔：`docs/*.md` 用英文（與 KNOWN_LIMITATIONS / NEXT_STEPS 一致），app.ts 字串與 zh-TW locale 用繁中。
- frontend 只用 yarn；agent 只用 npm（見 `docs/agents/environment.md`）。
- commit／push／開 PR 只在使用者同意後執行。**執行期裁定：** implementer 不 commit、不 stage；新檔用 `git add -N` 讓它出現在 diff。每個 task 審核通過後由 controller `git add` 該 task 的檔案作為檢查點。各 task 內的「Commit」步驟一律跳過。
- 行號以 2026-09-23 的 master 為準。前面的 task 插入內容後會位移，一律以步驟中引用的原文錨點定位，不要信行號。NEXT_STEPS 的新條目一律插在 `**Not in this workstream**` 段落之前，依 task 順序往下接。
- 完成後跑既有測試：`cd agent && npm test`、`cd frontend && yarn tsc --noEmit && yarn lint`。

## 已驗證的事實（2026-09-23，計畫中所有數字的來源）

### 程式碼

| 事實 | 位置 |
|---|---|
| facilitator 預設 `https://x402.org/facilitator`，env `X402_FACILITATOR_URL` | `agent/signal-api/src/app.ts:44-45` |
| x402-hono：verify → `await next()` → settle | `agent/node_modules/x402-hono/dist/esm/index.mjs:136 / 148 / 155` |
| handler 不再結算，只 `c.set("ledgerEntry")`；收到 `X-PAYMENT-RESPONSE` 後才入佇列 | `app.ts:249-284`（`applyLedgerRecording`）、`app.ts:651-681`、`app.ts:688-723` |
| worker 每筆 entry 各呼叫一次 `settleRevenue` → 一筆 `routeExternalRevenue` | `agent/signal-api/src/settlement-worker.ts:56-57, 100-122` |
| worker 每次 cron 最多處理 `SETTLEMENT_BATCH_SIZE`（預設 25）筆；cron `*/10 * * * *` | `settlement-worker.ts:43`、KNOWN_LIMITATIONS §14 |
| `_settle`：decimals、balanceOf、allowance（3 次 eth_call）→（allowance 不足才 approve）→ `routeExternalRevenue` → `tx.wait()` | `settlement.ts:107-155` |
| 分潤金額 = 該筆完整售價（`atomic = parseUnits(feeUsd)`），平台實得 20% | `settlement.ts:116`、`onchainRevenue.ts:4-5, 69-72` |
| payTo 預設 = FeeRouter，但 `.env.example` 指示設為 treasury EOA（= 結算錢包地址） | `agent/shared/src/env.ts:14-17`、`agent/.env.example:21-25` |
| `routeExternalRevenue(address trader, uint256 fee)`：一次只能給一個 trader | `contracts/src/FeeRouter.sol:127-132` |
| `maxTimeoutSeconds: MAX_TIMEOUT_SECONDS`（=60）已設在兩個付費路由 | `app.ts:55-62, 638, 645` |
| `/oracle/*` 新鮮度閘門在 paymentMiddleware 之前、**每個請求都跑**（含付款後重送） | `app.ts:590-624` |
| `/demo/buy-signal` 不結算：`settlementTx` 永遠 `undefined` | `app.ts:500-508` |
| `/revenue` 無事件掃描，`count` 永遠 `null` | `onchainRevenue.ts:63-68` |
| 前端把 `count: null` 顯示成 0 | `HeroKpiStrip.tsx:132, 147`、`X402DocsPage.tsx:75` |
| provider：`batchMaxCount: 1`、`staticNetwork: true`（不打 eth_chainId） | `agent/shared/src/provider.ts:33` |
| 每筆付費 `/oracle` 的 eth_call：閘門 2（402 時）＋ 閘門 2（付款重送）＋ `getOracleSnapshot` 6 ＋ `resolveTrader` 0–1 | `app.ts:595-598`、`agent/shared/src/aggregate.ts:239-246`、`app.ts:128-136` |

### 鏈上實測（Base Sepolia，Blockscout 公開 API，2026-09-23 讀取）

x402 FeeRouter `0x29e5732AC62254d9b92A1C7d3F38EbFA8809B57d` 自部署以來只有 4 筆 `routeExternalRevenue`（selector `0x12732157`），全部由 treasury `0xE80A81360608C1342e66743F70a00f75d792Eb93` 送出、每筆 fee = 10000（$0.01）：

| 日期 | trader | gasUsed | 總 fee (wei) | L1 fee (wei) | tx |
|---|---|---|---|---|---|
| 2026-06-15 | 0xE80A…Eb93 | 182,411（首次，冷 storage） | 1,187,252,925,986 | 92,786,925,986 | `0xe014eb3c…1dbef6` |
| 2026-06-16 | 0xE80A…Eb93 | 96,911 | 587,365,725,592 | 5,899,725,592 | `0x518cc7ae…c525e5` |
| 2026-07-15 | **0x0000…0000** | 113,771 | 688,370,397,758 | 5,744,397,758 | `0xfc61abce…ce1939` |
| 2026-07-15 | **0x0000…0000** | 96,671 | 586,102,398,505 | 6,076,398,505 | `0xba22cd50…d09189` |

x402 付款（EIP-3009 `transferWithAuthorization`，官方 USDC → payTo），**送交易、付 gas 的是 `0xd407e409E34E0b9afb99EcCeb609bDbcD5e7f1bf`（x402.org facilitator），不是我們**：

| 時間 (UTC) | 買方 | value | gasUsed | 總 fee (wei) | L1 fee (wei) | tx |
|---|---|---|---|---|---|---|
| 2026-06-22 13:00:08 | treasury 自付 | 5000 | 83,648 | 513,151,085,319 | 11,263,085,319 | `0xb8b9c9fe…344710` |
| 2026-06-23 02:43:58 | treasury 自付 | 5000 | 83,672 | 529,987,094,510 | 27,955,094,510 | `0x8561821d…3d38fb` |
| 2026-07-15 13:08:34 | `0x858b36C7…0bA972` | 10000 | 91,272 | 559,268,307,796 | 11,636,307,796 | `0x2590feb2…6d4` |
| 2026-07-15 13:12:16 | `0x858b36C7…0bA972` | 10000 | 91,272 | 560,699,683,875 | 13,067,683,875 | `0x9e0a6a04…90f81` |

- gas price 全部是 6,000,000 wei = **0.006 gwei**，與參考參數一致。
- 7/15 兩筆外部付款，對應的 `routeExternalRevenue` 在 13:08:30 / 13:12:12 上鏈——**比買方付款早 4 秒**。這是重構前「先分潤、後結算」順序問題的鏈上實例。
- 7/15 兩筆分潤的 trader 是零地址：70%（2 × 7000 = $0.014 測試 USDC）記進 `traderEarnings[0x0]`，永遠無法提領。現在 `/signals/*` 的零地址攔截（app.ts:577-586）擋住了這條路，但合約本身不檢查。
- EIP-3009 的實際 L1 fee（11.3–28.0 gwei）**高於**參考表的 8 gwei 預算（4.0 × 2）；`routeExternalRevenue` 的（5.7–6.1 gwei）在預算內。

### 參考參數（外部 facilitator 壓測 Excel，2026-09-04 假設日）

- Base：gas price 0.006 gwei × 1.3 = **0.0078 gwei/gas**；L1 data fee 4.0 gwei × 2 = **8 gwei/筆**；單筆預算 = `G × 0.0078 + 8` gwei。
- EIP-3009 預算 G = 120,000 → 944 gwei = 9.44E-07 ETH ≈ US$0.002。
- Ethereum：180,000 × 0.889 × 1.3 = 208,026 gwei = 2.08E-04 ETH。
- ETH = NT$78,057。美元換算採參考表自己的比例：944 gwei ≈ US$0.002 → **1 gwei ≈ US$2.1186E-06（隱含 ETH ≈ US$2,119）**。
- Alchemy CU：eth_call 26、eth_sendRawTransaction 40、eth_getTransactionReceipt 20。

---

## 檔案結構

| 檔案 | 動作 | 對應點 |
|---|---|---|
| `docs/COST_MODEL.md` | 新增 | 2a, 4b, 4c, 5 |
| `docs/KNOWN_LIMITATIONS.md` | 修改：總覽表、§14/§15/§16/§18 補述、新增 §19、§20 | 1, 3a, 4, 6, 7 |
| `docs/NEXT_STEPS.md` | 修改：Smaller open items 加 3 條、Related documents 加 COST_MODEL | 3c, 7, 8 |
| `agent/signal-api/src/app.ts` | 修改：`revenueModel` 字串、demo 路由註解與 `paymentInfo.note` | 2b, 7 |
| `agent/signal-api/scripts/probe-facilitator.ts` | 修改：新增 `MODE=concurrency` 分支 | 4a |
| `agent/README.md` | 修改：第 148 行 demo 描述 | 7 |
| `frontend/src/locales/{en,zh-TW}/x402.ts` | 修改：footer、tryBuy、card、split.callsUnknown | 1, 7 |
| `frontend/src/components/pepefi/HeroKpiStrip.tsx` | 修改：`count` 型別與 null 顯示 | 7 |
| `frontend/src/pages/pepefi/X402DocsPage.tsx` | 修改：`count` 型別與 null 顯示 | 7 |

**與原題不同的兩處，理由：**

1. **壓測不放 `benchmarks.ts`，放 `scripts/probe-facilitator.ts`。** `benchmarks.ts` 是 `GET /benchmarks`（S&P 500／黃金／BTC）的正式資料層，會被打包進 Vercel；KNOWN_LIMITATIONS §18 第 557 行已明寫它與付費路徑無關。`probe-facilitator.ts` 是既有的量測腳本，已有 `MODE=` 分支與 `summarize()`，加一個 MODE 即可，同樣不新增檔案。
2. **不量 `settleError` 裡的 nonce 錯誤。** 請求路徑已不送交易（§14），`settleError` 在請求路徑只可能是 Upstash 寫入失敗；nonce 衝突在設計上已消失（單一 worker）。壓測改記錄 HTTP 狀態分佈，並在文件說明為什麼。

---

### Task 0: 開工作分支

**Files:** 無

- [ ] **Step 1: 確認乾淨並從 master 開分支**

```bash
git status
git fetch origin master
git switch -c docs/x402-cost-limits origin/master
```

Expected: `git status` 顯示 clean；切到新分支。若不 clean，先 `git stash -u` 並告知使用者。

---

### Task 1: 第 1 點 — facilitator 來源與 gas 承擔者

**Files:**
- Modify: `docs/KNOWN_LIMITATIONS.md:24-28`（總覽表）、`:288`（區段說明）、在 §18 之後（第 585 行 `---` 之前）新增 §19
- Modify: `frontend/src/locales/en/x402.ts:67-68`
- Modify: `frontend/src/locales/zh-TW/x402.ts:66-67`

**Interfaces:** Produces：§19 標題 `## 19. No self-hosted facilitator — settlement gas is paid by x402.org`，Task 3、5 會引用「§19」。

- [ ] **Step 1: 總覽表加第 19 列**（接在第 28 行之後）

```markdown
| 19 | No self-hosted facilitator; x402.org pays the settlement gas | **By design (testnet)** — no SLA, no visibility into its wallet |
```

- [ ] **Step 2: 第 288 行 `Items 14–18 cover` 改為 `Items 14–20 cover`**

- [ ] **Step 3: 在 §18 結尾（第 584 行 `the paid path is claimed here.` 之後、第 586 行 `---` 之前）插入 §19**

```markdown
## 19. No self-hosted facilitator — settlement gas is paid by x402.org

`app.ts:44-45` defaults `X402_FACILITATOR_URL` to `https://x402.org/facilitator`,
and no deployment overrides it. That facilitator, not us, submits the buyer's
EIP-3009 `transferWithAuthorization` and pays its gas.

**On-chain evidence.** Every x402 payment into our `payTo` checked on Base
Sepolia was sent by `0xd407e409E34E0b9afb99EcCeb609bDbcD5e7f1bf`, not by any
key we hold — for example `0x2590feb2…6d4` and `0x9e0a6a04…90f81` (2026-07-15,
external buyer `0x858b36C7…0bA972`), gasUsed 91,272 each, and treasury self-pay
tests on 2026-06-22/23 at 83,648–83,672 gas. Full list in
[COST_MODEL.md](COST_MODEL.md#measured-on-chain).

**What that means:**

- **No SLA.** `docs.x402.org` describes the public facilitator as intended for
  development and testnet workflows (see §16). There is no uptime or latency
  commitment to rely on.
- **Testnet only.** It settles Base Sepolia. Nothing in this repository has been
  run against a mainnet facilitator.
- **We cannot monitor its wallet.** If `0xd407…f1bf` runs out of ETH, `/settle`
  fails. x402-hono then throws inside its settle block
  (`index.mjs:155-162`) and replaces our response with a 402: the buyer is not
  charged, and because the ledger only records on `X-PAYMENT-RESPONSE` (§14),
  no revenue split is queued either. The failure is safe, but we would only see
  it as a wave of 402s after valid payments — we have no balance alert on a
  wallet we do not own.
- **The cost is hidden, not zero.** On mainnet with a self-hosted facilitator,
  this gas becomes ours. [COST_MODEL.md](COST_MODEL.md) prices it.

**2026-09-23:** entry added; the `/x402` docs page footer now names the
facilitator and who pays its gas (`frontend/src/locales/{en,zh-TW}/x402.ts`).
```

- [ ] **Step 4: en footer（`frontend/src/locales/en/x402.ts:67-68`）**

```ts
    footer:
      'Testnet demo environment (Base Sepolia); the settlement key is for demo purposes only and holds no real assets. The on-chain revenue-split figures are read live. Payments are settled by the public x402.org facilitator, which pays that gas; this project does not run its own facilitator.',
```

- [ ] **Step 5: zh-TW footer（`frontend/src/locales/zh-TW/x402.ts:66-67`）**

```ts
    footer:
      '測試網展示環境（Base Sepolia）；結算金鑰僅供 demo，不涉及真實資產。鏈上分潤數字為即時讀取。付款由公開的 x402.org facilitator 結算並由它支付 gas；本專案未自架 facilitator。',
```

- [ ] **Step 6: 型別檢查**

Run: `cd frontend && yarn tsc --noEmit`
Expected: 無錯誤（zh-TW 與 en 的 key 結構不變）。

- [ ] **Step 7: Commit（使用者同意後）**

```bash
git add docs/KNOWN_LIMITATIONS.md frontend/src/locales/en/x402.ts frontend/src/locales/zh-TW/x402.ts
git commit -m "docs(x402): record that x402.org facilitator pays settlement gas (§19)"
```

---

### Task 2: 第 3、6 點 — 只記錄現況（結算順序、資金來源、maxTimeoutSeconds 依據）

**Files:**
- Modify: `docs/KNOWN_LIMITATIONS.md` §14（第 415 行 `Left open` 清單之後、第 417 行 `What batching did not fix` 之前）、§15（第 462 行之後）、§16（第 505-509 行）
- Modify: `docs/NEXT_STEPS.md`（`## Smaller open items` 內，第 244 行 `KNOWN_LIMITATIONS.md #12 has the detail.` 之後）

- [ ] **Step 1: 讀出實際的 `maxPriceAge`（Step 3 要寫進文件）**

先確認 `loadEnv`、`makeProvider`、`makeContracts` 都從 `@pepelab/shared` 匯出（`grep -n "export" agent/shared/src/index.ts`），再執行：

```bash
cd agent && npx tsx -e 'import {loadEnv,makeProvider,makeContracts} from "@pepelab/shared"; loadEnv(); (async()=>{ const c=makeContracts(makeProvider()); console.log("maxPriceAge="+String(await c.perp.maxPriceAge())); })()'
```

Expected: 印出 `maxPriceAge=<秒數>`。只記秒數，不要貼出任何 RPC URL。若 RPC 不通，Step 3 改寫成「讀取失敗」並保留論證（論證不依賴具體值，只依賴它 ≥ 60）。

- [ ] **Step 2: §14 補述（插在 `Left open` 清單最後一項之後）**

```markdown
**On-chain evidence of the old ordering.** Before the ledger change, the x402
FeeRouter recorded two routes for external buyer `0x858b36C7…0bA972` on
2026-07-15: `routeExternalRevenue` at 13:08:30 and 13:12:12 UTC, the buyer's
`transferWithAuthorization` at 13:08:34 and 13:12:16. **The split landed four
seconds before the payment it was splitting** — exactly the ordering described
above. Both routes also used trader `0x0`, so their 70% (2 × $0.007 test USDC)
sits in `traderEarnings[address(0)]` and can never be withdrawn. The zero-address
check in `app.ts:577-586` now blocks that input on `/signals`; `FeeRouter` itself
still accepts it.

**Where the split's money comes from.** It is still not the buyer's payment
moving through the router. The facilitator settles the buyer's USDC into `payTo`
(per `.env.example:21-25`, the treasury EOA that also holds
`FEE_SETTLEMENT_PRIVATE_KEY`). Later, the worker calls `routeExternalRevenue`,
which `safeTransferFrom`s an equal amount out of that same wallet
(`FeeRouter.sol:129`, `settlement.ts:116`). The amounts match and the order is
now correct, but it is two transactions minutes apart, not one atomic route.
If `PAY_TO` is ever set to an address other than the settlement wallet, the
worker pays the split from a balance the buyer never touched.

**2026-09-23:** evidence and funding source recorded. No code change: the
ordering was already fixed on 2026-09-17, and making the route atomic is a
`NEXT_STEPS.md` item, not a patch.
```

- [ ] **Step 3: §15 補述（第 462 行之後）**

```markdown
**Why 60 s and not something derived from `maxPriceAge`.** The two limits
protect different things. `maxPriceAge` (read live from
`PerpetualExchange.maxPriceAge()`, <N> s on 2026-09-23) decides whether a price
is still tradable. The `/oracle/*` freshness gate (`app.ts:590-624`) runs
before `paymentMiddleware` on **every** request, including the paid retry that
carries `X-PAYMENT`. So a buyer holding a 60-second authorization cannot be
sold a price older than `maxPriceAge`: if the price went stale in between, the
paid retry gets `503 price_stale` before `/verify` is ever called. The
requirement "no longer than the staleness threshold" holds because 60 s is far
below `maxPriceAge`; 60 was chosen from the response time of a single GET.

**2026-09-23:** rationale recorded; value unchanged.
```

（`<N>` 以 Step 1 的輸出取代；若讀取失敗，改寫為 `read live from PerpetualExchange.maxPriceAge(); the call failed on 2026-09-23` 並保留其餘文字。）

- [ ] **Step 4: §16 修正過時的一句（第 505-509 行）**

把
```markdown
  bare 402. Fixing it means patching or replacing the middleware. The buyer is
  not charged in this case, but see §14 on our revenue split having already run.
```
改為
```markdown
  bare 402. Fixing it means patching or replacing the middleware. The buyer is
  not charged in this case, and since §14's ledger change no revenue split is
  queued either (the entry is only recorded when `X-PAYMENT-RESPONSE` is present).
```

- [ ] **Step 5: NEXT_STEPS 加一條（原子路由）**

```markdown
**x402 revenue split is not atomic with the payment.** The buyer's USDC lands
in `payTo` via the facilitator; the settlement worker later routes an equal
amount out of the treasury wallet (`KNOWN_LIMITATIONS.md` §14). To make it one
transaction, `payTo` would have to be a contract that splits on receipt, which
the x402 v1 `exact` scheme does not support — `transferWithAuthorization` moves
tokens without calling the recipient. Options: an EIP-3009 `receiveWithAuthorization`
based scheme, or keep the two-step flow and reconcile `payTo` inflows against
routed totals. Not started.
```

- [ ] **Step 6: Commit（使用者同意後）**

```bash
git add docs/KNOWN_LIMITATIONS.md docs/NEXT_STEPS.md
git commit -m "docs(x402): record settlement ordering evidence, split funding source, maxTimeoutSeconds rationale"
```

---

### Task 3: 第 2、5 點 — COST_MODEL.md 成本段與 GET / 說明文字

**Files:**
- Create: `docs/COST_MODEL.md`
- Modify: `agent/signal-api/src/app.ts:339-344`

**Interfaces:** Produces：`docs/COST_MODEL.md` 內錨點 `#measured-on-chain`、`#capacity`（Task 1 與 Task 4 引用）；「每筆付費請求賣方 gas：testnet 763.9 gwei、mainnet 1,707.9 gwei」這兩個數字供 Task 6 引用。

- [ ] **Step 1: 建立 `docs/COST_MODEL.md`（成本段；容量段在 Task 4 補上實測數字）**

````markdown
# x402 Signal API — Cost Model

What one paid call costs the seller in gas, on today's testnet setup and on a
hypothetical Base mainnet deployment with a self-hosted facilitator. Every input
is either read from this repository, measured on Base Sepolia, or taken from the
external facilitator load-test sheet and labelled as such. Prices are unchanged
by this document: `/signals` $0.01, `/oracle` $0.005 (`app.ts:52-53`).

## How one paid call moves money

1. The buyer signs an EIP-3009 `transferWithAuthorization`. The **facilitator**
   submits it and pays its gas; USDC lands in `payTo` (`KNOWN_LIMITATIONS.md` §19).
2. Our middleware queues a ledger entry once the facilitator reports success
   (`app.ts:249-284`).
3. The settlement worker later sends **one `FeeRouter.routeExternalRevenue`
   transaction per entry** from the treasury wallet, which pays that gas
   (`settlement-worker.ts:56-57`, `settlement.ts:153-154`). It routes the full
   price: 70% trader, 20% platform, 10% vault (`settlement.ts:116`).
4. One-off: the first run needs an `approve(MaxUint256)` (`settlement.ts:147-151`).
   On the official-USDC router there is no `mint` — that path is MockUSDC-only
   (`settlement.ts:125-127`).

So each paid call is **two on-chain transactions**. Today the seller pays for
one of them; with a self-hosted facilitator it pays for both.

## Measured on-chain

Base Sepolia, read from the public Blockscout API on 2026-09-23. Gas price on
every transaction: 0.006 gwei.

| Transaction | Sender (pays gas) | gasUsed | Total fee | of which L1 fee |
|---|---|---|---|---|
| EIP-3009 settle, $0.005 | facilitator `0xd407…f1bf` | 83,648 – 83,672 | 513.2 – 530.0 gwei | 11.3 – 28.0 gwei |
| EIP-3009 settle, $0.01 | facilitator `0xd407…f1bf` | 91,272 | 559.3 – 560.7 gwei | 11.6 – 13.1 gwei |
| `routeExternalRevenue`, known trader | treasury | 96,671 – 96,911 | 586.1 – 587.4 gwei | 5.7 – 6.1 gwei |
| `routeExternalRevenue`, new trader slot | treasury | 113,771 | 688.4 gwei | 5.7 gwei |
| `routeExternalRevenue`, first ever call | treasury | 182,411 | 1,187.3 gwei | 92.8 gwei |

The x402 FeeRouter (`0x29e5732AC62254d9b92A1C7d3F38EbFA8809B57d`) has exactly
four `routeExternalRevenue` calls in its history. Transactions used above:

- `routeExternalRevenue` (sender treasury `0xE80A81360608C1342e66743F70a00f75d792Eb93`):
  - 2026-06-15 `0xe014eb3c90f62a79b49efa0d28fc07b56ac7477272a99aab2507983b181dbef6` (182,411 gas)
  - 2026-06-16 `0x518cc7ae4f3b16d03e18b937fc5246df0d0928d1a81cfbc7da0431a50bc525e5` (96,911 gas)
  - 2026-07-15 `0xfc61abce56f3c4cc1be4c4efb3e8d140999701f13ccf56d35a43bfa221ce1939` (113,771 gas, trader `0x0`)
  - 2026-07-15 `0xba22cd5027e7992f41ef650b8ebae85a0797f1352f02a991c0861fc163d09189` (96,671 gas, trader `0x0`)
- EIP-3009 `transferWithAuthorization` (sender facilitator `0xd407e409E34E0b9afb99EcCeb609bDbcD5e7f1bf`):
  - 2026-06-22 `0xb8b9c9fe045536172f1a5c36c80e2ccc1919230b6a63aa531d409c589c344710` ($0.005, 83,648 gas)
  - 2026-06-23 `0x8561821d0ea7a1cf68c2f74674c73841a09545bb350b81af47692c22e03d38fb` ($0.005, 83,672 gas)
  - 2026-07-15 `0x2590feb209b0dc15b8578aa96db3d4121d6b71f1880f62369014473b987456d4` ($0.01, 91,272 gas)
  - 2026-07-15 `0x9e0a6a048111de0d2c8a70cdbce83c4083d471a94c34de6d39679a2a80b90f81` ($0.01, 91,272 gas)

The measured EIP-3009 L1 fee (11–28 gwei) is **above** the load-test sheet's
8 gwei budget; the router's (≈6 gwei) is within it.

## Assumptions

| Symbol | Value | Source |
|---|---|---|
| Effective L2 gas price | 0.006 gwei × 1.3 = 0.0078 gwei | load-test sheet; 0.006 matches every tx above |
| L1 data fee per tx | 4.0 gwei × 2 = 8 gwei | load-test sheet |
| Per-tx budget | `gasUsed × 0.0078 + 8` gwei | load-test sheet's method |
| EIP-3009 budget gas | 120,000 | load-test sheet (upper bound of 110k–120k) |
| `routeExternalRevenue` gas | 96,911 | measured, known-trader case above |
| ETH → NT$ | 78,057 | load-test sheet |
| ETH → US$ | 2,119 | implied by the sheet's "9.44E-07 ETH ≈ US$0.002" (1 gwei ≈ US$2.1186E-06) |
| Platform take | 20% of price | `onchainRevenue.ts:4-5` |

The US$ rate is the sheet's own rounding, not a market quote. Every US$ figure
below scales linearly with it.

## Per-transaction cost

| Transaction | Formula | gwei | ETH | NT$ | US$ |
|---|---|---|---|---|---|
| EIP-3009 settle (budget) | 120,000 × 0.0078 + 8 | 944.0 | 9.44E-07 | 0.0737 | 0.00200 |
| `routeExternalRevenue` (budget) | 96,911 × 0.0078 + 8 | 763.9 | 7.64E-07 | 0.0596 | 0.00162 |
| EIP-3009 settle (measured max, no buffer) | — | 560.7 | 5.61E-07 | 0.0438 | 0.00119 |
| `routeExternalRevenue` (measured, no buffer) | — | 587.4 | 5.87E-07 | 0.0458 | 0.00124 |
| Ethereum L1 EIP-3009, for comparison | 180,000 × 0.889 × 1.3 | 208,026 | 2.08E-04 | 16.24 | 0.441 |

Ethereum is ≈220× the Base budget (208,026 / 944). Nothing below assumes Ethereum.

## Price vs seller gas, per endpoint

Seller gas per paid call:

- **Testnet today:** the router transaction only = 763.9 gwei (budget). The
  facilitator pays the other one. Testnet ETH has no market value, so the real
  cost is $0; the column prices it at the mainnet assumptions to show the shape.
- **Base mainnet, self-hosted facilitator:** EIP-3009 + router = 944.0 + 763.9 =
  1,707.9 gwei (budget) = US$0.00362.

| | `/signals` | `/oracle` |
|---|---|---|
| Price | $0.01 | $0.005 |
| Platform's 20% | $0.002 | $0.001 |
| **Testnet today** — seller gas (router tx, mainnet-priced) | $0.00162 (16.2% of price) | $0.00162 (32.4% of price) |
| **Base mainnet, self-hosted** — seller gas (both txs, budget) | $0.00362 (36.2% of price) | $0.00362 (72.4% of price) |
| Platform net per call on mainnet (20% − gas, budget) | **−$0.00162** | **−$0.00262** |
| Same, measured fees without buffers (1,148.1 gwei = $0.00243) | −$0.00043 | −$0.00143 |

The seller absorbs all gas while keeping only 20% of the price, because the
split routes the full amount (`settlement.ts:116`). **At these assumptions, a
self-hosted mainnet deployment loses money on every paid call on both
endpoints.** This is a statement about the cost structure, not a pricing
proposal.

## If this moves to Base mainnet

- **Faucet rate limits disappear.** `agent/.env.example` funds testing from the
  Base faucet (ETH) and Circle's faucet (USDC). On mainnet both are bought, so
  that operational limit goes away.
- **Both transactions become real costs.** Using the public facilitator is a
  testnet-only option (§19); self-hosting means paying the EIP-3009 gas in the
  table above in addition to the router gas we already pay.
- **Batching helps only the router half.** See `NEXT_STEPS.md`, "x402 settlement
  is one transaction per payment". The EIP-3009 transaction is per payment by
  construction of the x402 v1 `exact` scheme.

## Capacity

Filled in by the concurrency run in Task 4.
````

- [ ] **Step 2: 更新 `GET /` 的 `revenueModel`（`app.ts:339-344`）**

把
```ts
    revenueModel:
      `x402 付款直接進 payTo（${PAY_TO}）；70/20/10 分潤是平台另外的一筆交易，` +
      `經 FeeRouter.routeExternalRevenue 上鏈，累計可於 /revenue 查詢。` +
      `兩者是不同的兩筆交易。` +
      `2026-09-17 起分潤改為非同步：回應裡的 settled 代表「已排入結算佇列」，` +
      `不代表已經上鏈；由單一 worker 定期批次結算（見 docs/KNOWN_LIMITATIONS.md §14）。`,
```
改為
```ts
    revenueModel:
      `x402 付款直接進 payTo（${PAY_TO}），這筆 EIP-3009 交易由 facilitator（${FACILITATOR_URL}）` +
      `送出並支付 gas。70/20/10 分潤是平台另外的一筆 FeeRouter.routeExternalRevenue 交易，` +
      `由結算錢包（FEE_SETTLEMENT_PRIVATE_KEY）送出並支付 gas，累計可於 /revenue 查詢。` +
      `兩者是不同的兩筆交易。` +
      `2026-09-17 起分潤改為非同步：回應裡的 settled 代表「已排入結算佇列」，` +
      `不代表已經上鏈；由單一 worker 定期取出，每筆各送一筆交易` +
      `（見 docs/KNOWN_LIMITATIONS.md §14、docs/COST_MODEL.md）。`,
```

「批次結算」改成「每筆各送一筆交易」：worker 是批次**取出**，但每筆 entry 仍各自上鏈（`settlement-worker.ts:114-115`），原文會讓人以為已經合併路由。

- [ ] **Step 3: 跑 agent 測試（確認字串改動沒弄壞 bundle 指紋等檢查）**

Run: `cd agent && npm test`
Expected: 全部通過。若 `bundleFingerprint.test.mjs` 因 `api/index.js` 需重新 bundle 而失敗，依錯誤訊息執行 `cd agent/signal-api && npm run bundle:vercel` 後重跑，並把 `api/index.js` 一起納入 commit。

- [ ] **Step 4: Commit（使用者同意後）**

```bash
git add docs/COST_MODEL.md agent/signal-api/src/app.ts
git commit -m "docs(x402): add COST_MODEL.md and state who pays each settlement tx's gas in GET /"
```

---

### Task 4: 第 4 點 — 併發量測、CU 估算、容量段

**Files:**
- Modify: `agent/signal-api/scripts/probe-facilitator.ts:1-19`（檔頭說明）、第 138 行 `}` 之後插入新分支
- Modify: `docs/COST_MODEL.md`（`## Capacity` 段）
- Modify: `docs/KNOWN_LIMITATIONS.md` §18（第 584 行之後）

**Interfaces:** Consumes：`probe-facilitator.ts` 既有的 `summarize(name, samples)`（第 97-106 行）與 `MODE`、`SAMPLES`。

- [ ] **Step 1: 檔頭說明加一行（第 5 行之後）**

```ts
//   cd agent && MODE=concurrency SAMPLES=20 API_URL=http://localhost:4021 npx tsx signal-api/scripts/probe-facilitator.ts
```

並在第 12 行之後加：

```ts
// concurrency：對 /oracle/sBTC（可用 BENCH_PATH 覆寫）以 1、5、10 併發各打 SAMPLES 次，
//   **不帶 X-PAYMENT**——量的是「新鮮度閘門 2 次 RPC + 402 挑戰」這段在併發下的表現，
//   不付款、不動任何 USDC。完整付費請求與結算 worker 的容量見 docs/COST_MODEL.md。
```

- [ ] **Step 2: 在 `challenge-latency` 分支之後（第 138 行之後）插入**

```ts
if (MODE === "concurrency") {
  const api = (process.env.API_URL ?? "http://localhost:4021").replace(/\/$/, "");
  const path = process.env.BENCH_PATH ?? "/oracle/sBTC";
  for (const level of [1, 5, 10]) {
    const out: { status: number; ms: number }[] = [];
    let issued = 0;
    const t0All = performance.now();
    await Promise.all(
      Array.from({ length: level }, async () => {
        while (issued < SAMPLES) {
          issued += 1;
          const t0 = performance.now();
          try {
            const res = await fetch(api + path, { headers: { Accept: "application/json" } });
            await res.arrayBuffer();
            out.push({ status: res.status, ms: Math.round(performance.now() - t0) });
          } catch {
            out.push({ status: 0, ms: Math.round(performance.now() - t0) });
          }
        }
      }),
    );
    const wallSec = (performance.now() - t0All) / 1000;
    summarize(`concurrency=${level} ${api}${path}`, out);
    console.log(`  throughput=${(out.length / wallSec).toFixed(2)} req/s wall=${wallSec.toFixed(2)}s`);
  }
  process.exit(0);
}
```

`status: 0` 代表連線層失敗（`summarize` 的 status 分佈會顯示）。

- [ ] **Step 3: 啟動本機 signal-api（背景執行）**

```bash
cd agent/signal-api && npm run start
```

用 Bash `run_in_background`。等到 log 出現監聽 4021 後，確認：

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:4021/healthz
```

Expected: `200`。若 `agent/.env` 缺 RPC，閘門的 RPC 讀取會失敗並放行（`app.ts:620-622`），此時量到的是「無 RPC」路徑——必須在結果中註明，不可當成正常數據。

- [ ] **Step 4: 跑量測**

```bash
cd agent && MODE=concurrency SAMPLES=20 API_URL=http://localhost:4021 npx tsx signal-api/scripts/probe-facilitator.ts
```

Expected: 三行 `concurrency=1/5/10 … p50=… p95=… status={…}` 各接一行 throughput。預期狀態是 402（價格新鮮）或 503（`price_stale`，§18 記載 oracle 可能已多日未更新）；兩者都經過 2 次 RPC，都算閘門正常運作。量完停掉背景 server。

- [ ] **Step 5: 數 `/signals` 的 RPC 次數**

讀 `agent/shared/src/aggregate.ts:333` 起的 `getTraderPerformance`，列出每個 `c.xxx.yyy()` 呼叫；若有依倉位數迴圈，寫成「固定 a 次 + 每個倉位 b 次」。這個結果寫進 Step 7 的表格。

- [ ] **Step 6: 確認 worker 送交易時 ethers 額外發出的 RPC 方法**

讀 `agent/node_modules/ethers/lib.esm/providers/abstract-signer.js`（`populateTransaction`、`sendTransaction`）與 `abstract-provider.js`（`getFeeData`、`broadcastTransaction`、`waitForTransaction`），列出 `routeExternalRevenue` 一次送出會觸發的 JSON-RPC 方法。參考 CU 表沒有的方法（例如 `eth_estimateGas`、`eth_getTransactionCount`、`eth_getBlockByNumber`、`eth_blockNumber`、`eth_maxPriorityFeePerGas`）照列、不計價，並標為「下限未含」。

- [ ] **Step 7: 以量測結果取代 COST_MODEL.md 的 `## Capacity` 段**

````markdown
## Capacity

**Not load-tested to 100 TPS.** The bottleneck is this service's serialized
settlement, not the Base chain.

### Settlement worker ceiling (from configuration, not measured)

The worker takes at most `SETTLEMENT_BATCH_SIZE` entries per run (default 25,
`settlement-worker.ts:43`) and runs on `*/10 * * * *` (§14): **150 settled
payments per hour ≈ 0.04 per second.** Above that sustained rate the queue grows
without bound — entries are not lost, they wait. Raising the batch size does not
remove the ceiling: entries are processed one after another, each awaiting its
own receipt (`settlement.ts:154`), so one signer cannot exceed roughly one
settlement per Base block (≈2 s), i.e. ≈0.5/s, before counting RPC round-trips.
100 TPS would be ~200× that.

### Request path

Every paid request also waits for the facilitator's `/settle`, which waits for
the EIP-3009 receipt (x402-hono `index.mjs:155`), so a paid call cannot return
faster than one Base block. The facilitator's own rate limit is unknown (§16).

Concurrency run, 2026-09-23, `probe-facilitator.ts MODE=concurrency`, 20
requests per level, unpaid `GET /oracle/sBTC` against a local server (this is
the freshness gate's 2 RPC reads + the 402 challenge; no payment, no USDC moved):

| Concurrency | n | p50 | p95 | Throughput | Status distribution |
|---|---|---|---|---|---|
| 1 | 20 | … ms | … ms | … req/s | {…} |
| 5 | 20 | … ms | … ms | … req/s | {…} |
| 10 | 20 | … ms | … ms | … req/s | {…} |

(Replace each `…` with the script's output. State whether the gate returned 402
or 503 `price_stale`, and whether the local `.env` had a working RPC.)

`settleError` is not counted: since §14 the request path sends no transactions,
so `nonce too low` / `replacement transaction underpriced` cannot occur there.
The only `settleError` the request path can produce is an Upstash write failure.

### Alchemy CU per request

`batchMaxCount: 1` (`provider.ts:33`) sends every call as its own HTTP request;
`staticNetwork: true` means no `eth_chainId` probe.

**Request path, one paid `/oracle` call** (the 402 challenge plus the paid retry):

| Step | Method | Count | Source |
|---|---|---|---|
| Freshness gate on the 402 challenge | eth_call | 2 | `app.ts:595-598` |
| Freshness gate again on the paid retry | eth_call | 2 | same middleware, every request |
| `getOracleSnapshot` | eth_call | 6 | `aggregate.ts:239-246` |
| `resolveTrader` (only if `DEMO_TRADER_ADDRESS` is unset) | eth_call | 0–1 | `app.ts:132` |
| **Total** | | **10–11 × 26 = 260–286 CU** | |

**Request path, one paid `/signals` call:** (Step 5 result.)

**Settlement worker, per entry** (steady state: allowance already set):

| Method | Count | CU |
|---|---|---|
| eth_call (`decimals`, `balanceOf`, `allowance`) | 3 | 78 |
| eth_sendRawTransaction | 1 | 40 |
| eth_getTransactionReceipt | ≥ 1 | ≥ 20 |
| (Step 6 list: methods the sheet does not price) | … | not counted |
| **Lower bound** | | **≥ 138 CU** |

Plus one `FeeRouter.usdc()` eth_call per worker run (`settlement.ts:89`, cached).

**At 10 paid `/oracle` calls per second:** request path 2,600–2,860 CUPS. The
worker could not keep up at that rate (ceiling above); if it could, it would
add ≥ 1,380 CUPS.
````

- [ ] **Step 8: KNOWN_LIMITATIONS §18 結尾加指向**（§18 最後一段 `the paid path is claimed here.` 之後、`## 19.` 標題之前）

```markdown
**2026-09-23:** a concurrency run of the 402 challenge (1/5/10 concurrent) and
the settlement worker's throughput ceiling (150/hour at default settings) are in
[COST_MODEL.md](COST_MODEL.md#capacity). The paid path is still unmeasured.
```

- [ ] **Step 9: 跑 agent 測試**

Run: `cd agent && npm test`
Expected: 全部通過（`probe-facilitator.ts` 不在測試清單內，只需確認沒有破壞其他東西）。另跑 `cd agent && npx tsc --noEmit -p signal-api`；若該 tsconfig 不涵蓋 `scripts/`，以 Step 4 能執行為準。

- [ ] **Step 10: Commit（使用者同意後）**

```bash
git add agent/signal-api/scripts/probe-facilitator.ts docs/COST_MODEL.md docs/KNOWN_LIMITATIONS.md
git commit -m "docs(x402): add concurrency probe, CU estimate and settlement capacity ceiling"
```

---

### Task 5: 第 7 點 — demo 文案與 KPI 假零值

**Files:**
- Modify: `agent/signal-api/src/app.ts:466`（註解）、`:516`（`paymentInfo.note`）
- Modify: `agent/README.md:148`
- Modify: `frontend/src/locales/en/x402.ts:39, 50-52, 78-79`
- Modify: `frontend/src/locales/zh-TW/x402.ts:39, 50-52, 77-78`
- Modify: `frontend/src/components/pepefi/HeroKpiStrip.tsx:23, 147`
- Modify: `frontend/src/pages/pepefi/X402DocsPage.tsx:19, 74-76`
- Modify: `docs/KNOWN_LIMITATIONS.md`（總覽表、§19 之後新增 §20）
- Modify: `docs/NEXT_STEPS.md`

**為什麼不照原題「過濾 msg.sender == treasury」：** 鏈上 4 筆 `routeExternalRevenue` 全部由 treasury 送出，現在的 worker 也用同一把 treasury key（`settlement.ts:44`）。以 sender 過濾會把**所有**收入都算成 demo。能區分的依據是「該筆分潤有沒有對應的外部 `transferWithAuthorization`」，需要事件掃描＝新函式，列入 NEXT_STEPS。

- [ ] **Step 1: app.ts demo 路由註解（第 466 行）**

```ts
  // ── 免費 demo：訪客不需自帶錢包；不付款、不結算，只回真實訊號（原因見下方註解） ──
```

- [ ] **Step 2: app.ts `paymentInfo.note`（第 516 行）**

```ts
          note: "demo：免費、不付款、不結算；真實外部 agent 自帶錢包經 x402 付費（見 /）",
```

- [ ] **Step 3: agent/README.md 第 148 行**

```markdown
`POST /demo/buy-signal`：免費 demo，**不付款、不結算**——只回真實訊號；`settlementTx` 永遠為空（在請求內上鏈會讓 serverless 逾時，見 `app.ts` 該路由註解）。真實 70/20/10 分潤只來自付費 x402 端點，累計見 `/revenue`。前端 `/x402` 文件頁與 Marketplace 卡片都接這支（含簡易速率限制 `DEMO_COOLDOWN_MS`，預設 15s/IP）。
```

- [ ] **Step 4: en locale**

`split` 區塊（第 39 行 `calls` 之後）加：
```ts
      callsUnknown: 'call count not tracked on-chain',
```
`tryBuy`（第 50-52 行）：
```ts
      description:
        'Click to fetch a live signal for free. The demo pays nothing and settles nothing: no USDC moves and no 70/20/10 transaction is sent. Real revenue comes only from the paid x402 endpoints below, and its on-chain total is shown above. (A real external agent brings its own wallet — see the example below.)',
      busy: 'Fetching…',
      cta: 'Try a signal (free)',
```
`card`（第 78-79 行）：
```ts
    busy: 'Fetching…',
    tryBuy: 'Try free',
```

- [ ] **Step 5: zh-TW locale**

`split`（第 39 行之後）：
```ts
      callsUnknown: '鏈上未記錄呼叫次數',
```
`tryBuy`（第 49-52 行）：
```ts
      description:
        '免費取得一筆即時訊號。demo 不付款、也不結算：不動任何 USDC、不送 70/20/10 交易。真實收入只來自下方的付費 x402 端點，其鏈上累計即上方數字。（真實外部 agent 則自帶錢包，見下方範例。）',
      busy: '讀取中…',
      cta: '免費試用一筆訊號',
```
`card`（第 77-78 行）：
```ts
    busy: '讀取中…',
    tryBuy: '免費試用',
```

`tryBuy.settled`、`viewSettlement`、`card.settled` 保留：對應的 UI 以 `result.settlementTx &&` 守住（`X402DocsPage.tsx:228`），現在不會顯示，但不是錯誤文案。

- [ ] **Step 6: HeroKpiStrip.tsx**

第 23 行 `count: number` → `count: number | null`。

第 147 行：
```tsx
          <Mono tone="gold">{rev?.count == null ? '—' : Math.round(calls).toLocaleString()}</Mono>
```
第 132 行 `useCountUp(rev?.count ?? 0)` 不動（hook 必須無條件呼叫）。

- [ ] **Step 7: X402DocsPage.tsx**

第 19 行 `count: number` → `count: number | null`。

第 74-76 行：
```tsx
          <Num tone="muted">
            {rev?.count == null
              ? t.x402.docs.split.callsUnknown
              : interpolate(t.x402.docs.split.calls, { count: rev.count })}
          </Num>
```

- [ ] **Step 8: KNOWN_LIMITATIONS 總覽表加第 20 列，並在 §19 之後新增 §20**

```markdown
| 20 | On-chain revenue totals cannot separate demo self-payments from external ones | **Open** — documented; needs an event scan |
```

```markdown
## 20. On-chain revenue totals cannot separate demo self-payments from external ones

`/revenue` reads `FeeRouter.platformEarnings()` and multiplies (`onchainRevenue.ts:40-73`).
It has no event scan, so it cannot say which routes came from whom, and it
reports `count: null` rather than a number.

**What the total contains.** The x402 FeeRouter has four `routeExternalRevenue`
calls, all $0.01, all sent by the treasury (see [COST_MODEL.md](COST_MODEL.md#measured-on-chain)).
Two match external payments from `0x858b36C7…0bA972` on 2026-07-15. The two
from June 15–16 name the treasury itself as trader; whether an external payment
preceded them was not checked (the transfer listing read on 2026-09-23 did not
reach back that far). Separately, many `/oracle` payments on 2026-06-22/23
were the treasury paying itself through x402 and were never routed at all
(`/oracle` did not settle then — `app.ts:710-711`). So the headline "x402
Revenue" mixes self-paid demo activity with real external revenue, and the
number of paid calls is unknown.

**Why filtering by sender does not work.** Every route, demo or real, is sent by
the same treasury key; the current worker uses it too (`settlement.ts:44`).

**What is no longer true.** `/demo/buy-signal` no longer settles anything
(`app.ts:500-508`), so it cannot add to the total going forward. The docs page
and README used to say it paid $0.01 and returned a real settlement tx; that
copy was wrong and is fixed.

**2026-09-23:** demo copy corrected in `frontend/src/locales/{en,zh-TW}/x402.ts`,
`agent/README.md` and the `/demo/buy-signal` comment and `paymentInfo.note`. The
home-page KPI and the docs page showed `count: null` as "0 calls"; they now show
"—" / "call count not tracked on-chain". No attribution field was added to
`/revenue`: doing it honestly needs an event scan (`NEXT_STEPS.md`).
```

- [ ] **Step 9: NEXT_STEPS 加一條**

```markdown
**Attribute x402 revenue by payer.** `/revenue` cannot tell demo self-payments
from external ones (`KNOWN_LIMITATIONS.md` §20). The workable signal is not the
router's `msg.sender` (always the treasury) but whether each
`ExternalRevenueRouted` event has a matching `transferWithAuthorization` into
`payTo` from a non-treasury address. That needs a log scan over the router and
USDC, with a block cursor so it is incremental. Not started.
```

- [ ] **Step 10: 型別與 lint**

Run: `cd frontend && yarn tsc --noEmit && yarn lint`
Expected: 無錯誤。

- [ ] **Step 11: 瀏覽器驗證**

`preview_start {name: "pepefi-dev"}`（port 8081）。檢查：
1. 首頁 KPI「Agent Calls Paid」顯示 `—`（`/revenue` 回 `count: null` 時）。若本機 `VITE_SIGNAL_API_URL` 不通，tile 會停在未載入狀態——註明「API 未連上，無法驗證 null 分支」，不要宣稱通過。
2. `/x402` 頁：分潤條右上顯示「call count not tracked on-chain」；試用卡片文案為新版；按鈕為「Try a signal (free)」；footer 有 facilitator 一句。
3. 切 zh-TW 重看一次。
4. `read_console_messages` 無新錯誤。
截圖作為證據。

- [ ] **Step 12: agent 測試**

Run: `cd agent && npm test`
Expected: 全部通過（必要時同 Task 3 Step 3 重新 bundle）。

- [ ] **Step 13: Commit（使用者同意後）**

```bash
git add agent/signal-api/src/app.ts agent/README.md frontend/src/locales/en/x402.ts frontend/src/locales/zh-TW/x402.ts frontend/src/components/pepefi/HeroKpiStrip.tsx frontend/src/pages/pepefi/X402DocsPage.tsx docs/KNOWN_LIMITATIONS.md docs/NEXT_STEPS.md
git commit -m "fix(x402): demo copy no longer claims it pays or settles; show unknown call count as unknown"
```

---

### Task 6: 第 8 點 — 為什麼不 batch

**Files:**
- Modify: `docs/NEXT_STEPS.md`（`## Smaller open items`）、`## Related documents`（第 284 行起）

- [ ] **Step 1: 加一條**

```markdown
**x402 settlement is one transaction per payment — deliberately, for now.** The
worker drains the queue in batches but still sends one `routeExternalRevenue`
per entry (`settlement-worker.ts:114-115`). In the PoC that is on purpose: the
demo can point at one settlement transaction per sale. `FeeRouter` does not
need to change to batch — `routeExternalRevenue(trader, fee)` accepts any fee,
so entries can be summed per trader and routed once every N entries or T
seconds.

When it is worth doing, from [COST_MODEL.md](COST_MODEL.md): per paid call the
router transaction costs 763.9 gwei (budget) against an EIP-3009 settle of
944.0 gwei. Batching N entries for one trader divides only the router part by N;
the EIP-3009 part is per payment. On a self-hosted mainnet deployment:

- `/signals` (platform keeps $0.002 ≈ 944 gwei): with measured, unbuffered fees
  (EIP-3009 560.7 + router 587.4/N gwei) it breaks even at **N ≥ 2**; at budget
  figures it never does.
- `/oracle` (platform keeps $0.001 ≈ 472 gwei): the EIP-3009 settle alone
  exceeds it, so **no batch size breaks even**.

Batch once paid volume exceeds the worker ceiling (150/hour, COST_MODEL
"Capacity") or before any mainnet deployment, whichever comes first. Batching
routes per trader, so it helps least when every call names a different trader.
```

- [ ] **Step 2: Related documents 加一行**

```markdown
- `docs/COST_MODEL.md` — x402 gas per paid call, testnet vs mainnet, CU and capacity
```

- [ ] **Step 3: Commit（使用者同意後）**

```bash
git add docs/NEXT_STEPS.md
git commit -m "docs(x402): record why settlement is not batched yet and the break-even threshold"
```

---

### Task 7: 收尾 — 全部測試、交叉檢查、PR 描述

- [ ] **Step 1: 全部測試**

```bash
cd agent && npm test
cd frontend && yarn tsc --noEmit && yarn lint && yarn test
```

Expected: 全部通過。貼出摘要行作為證據。

- [ ] **Step 2: 敏感值掃描**

```bash
git diff origin/master --stat
git diff origin/master | grep -nEi "alchemy\.com/v2/|infura\.io/v3/|PRIVATE_KEY=0x|0x[0-9a-f]{64}\b" || echo "clean"
```

Expected: `clean`，或只命中文件內的交易 hash（64 hex 的 tx hash 是公開資料，逐一確認不是私鑰）。

- [ ] **Step 3: 文案一致性交叉檢查**

```bash
git grep -nE "代付|cover a \$0.01|real settlement tx|批次結算" -- frontend/src agent/signal-api/src agent/README.md
```

Expected: 不再命中 demo 相關的錯誤描述。

- [ ] **Step 4: PR 描述（≤ 15 行）**

```markdown
## x402 成本、結算與容量說明（Cost & Limits）

1. facilitator：新增 KNOWN_LIMITATIONS §19（x402.org 付 EIP-3009 gas，鏈上可見 `0xd407…f1bf`）；/x402 footer 補來源。未自架。
2. 兩筆鏈上交易：新增 docs/COST_MODEL.md（鏈上實測 gas + 參考參數試算）；GET / 寫明兩筆交易各由誰付 gas。未改定價——但表中顯示 mainnet 自架時兩端點每筆都虧。
3. 結算順序：2026-09-17 已修（ledger + worker）。本輪只補 §14 鏈上證據（分潤比付款早 4 秒）與資金來源；§16 過時一句修正；原子路由列 NEXT_STEPS。未加 boolean 欄位（舊架構已不存在）。
4. 容量：probe-facilitator.ts 加 MODE=concurrency（不付款）；COST_MODEL 容量段：worker 上限 150 筆/小時、CU 每筆 /oracle 260–286。未壓到 100 TPS。壓測沒放 benchmarks.ts（那是正式 /benchmarks 資料層）。
5. mainnet：COST_MODEL「If this moves to Base mainnet」段。
6. maxTimeoutSeconds：早已設 60（§15）。只補「為何不必綁 maxPriceAge」的論證，值未改。
7. demo：/demo/buy-signal 早已不結算，但前端與 README 仍說「代付並回 settlement tx」——已改正；KPI 把 count:null 顯示成 0 的假值改為「—」。未照原題用 msg.sender 拆分（全部都是 treasury 送的），列 §20 + NEXT_STEPS。
8. batch：NEXT_STEPS 記錄刻意逐筆的原因與門檻（/signals N≥2 才打平，/oracle 無解）。

測試：agent `npm test`、frontend `tsc` / `lint` / `vitest` 全過；/x402 與首頁 KPI 已在瀏覽器確認。
```

（最後一行依實際結果改寫；若某項未驗證就照實寫。）

- [ ] **Step 5: 交給使用者決定是否 push／開 PR**

---

## Self-Review

**Spec coverage：**

| 原題 | 對應 |
|---|---|
| 1 KNOWN_LIMITATIONS 條目 + footer | Task 1 |
| 2a COST_MODEL 售價 vs gas、兩欄、計算式 | Task 3 Step 1 |
| 2b GET / 補 gas 承擔者 | Task 3 Step 2 |
| 2c 不改定價 | Global Constraints |
| 3a 順序說明 | Task 2 Step 2（依現況改寫為歷史證據 + 資金來源）、Step 4 |
| 3b boolean 欄位 | 使用者決議不加；PR 描述說明 |
| 3c NEXT_STEPS | Task 2 Step 5 |
| 4a 併發量測 1/5/10 × 20、p50/p95、成功率 | Task 4 Step 2-4（放 probe-facilitator.ts，理由見檔案結構） |
| 4b 容量段、「未壓測至 100 TPS；瓶頸在序列化結算」 | Task 4 Step 7 |
| 4c CU 方法與次數、10 req/s CUPS | Task 4 Step 5-7 |
| 5 mainnet 段 | Task 3 Step 1 |
| 6 maxTimeoutSeconds | 已完成；Task 2 Step 3 補依據 |
| 7 demo vs 外部、KPI 文案 | Task 5（依現況：修錯誤文案 + 假零值；拆分列 NEXT_STEPS） |
| 8 NEXT_STEPS batch | Task 6 |
| 改動摘要附在 KNOWN_LIMITATIONS 對應條目 | 各條目的 `**2026-09-23:**` 段 |
| 跑既有測試 | Task 3/4/5 各自 + Task 7 |
| PR 描述 ≤ 15 行 | Task 7 Step 4 |

**佔位檢查：** 刻意保留、需執行時填入的只有量測值：Task 2 的 `<N>`（maxPriceAge）、Task 4 容量表的 `…`、Step 5/6 的清單。每處都寫明了產生該值的指令與失敗時的寫法。

**名稱一致性：** §19、§20、`#measured-on-chain`、`#capacity`、`split.callsUnknown`、`MODE=concurrency` 在各 Task 中拼法一致；763.9 / 944.0 / 1,707.9 gwei 與 Task 6 引用一致。
