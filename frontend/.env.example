# ── 前端 build 設定 ────────────────────────────────────────────────────────────
# 這個 build 出貨的介面語言：zh-TW（預設）或 en。認不出來的值會退回 zh-TW 並在 console
# 留一行 warn。語言是建置期決定的，app 內沒有切換器——要英文站就用同一份 code 再 build
# 一次（例如第二個 Vercel project 只改這個變數），不需要開分支。
#
#   VITE_LOCALE=en yarn build          # bash
#   $env:VITE_LOCALE='en'; yarn build  # PowerShell
#
# 寫在這裡（.env.local）或設成真正的環境變數都可以，兩種來源 index.html 與 app 都讀得到。
VITE_LOCALE=zh-TW

# 下單面板要不要露出槓桿選擇器。預設 **關**——平台的門面是代幣化 RWA 的現貨買賣，
# 一進站就看到 5× 按鈕會讓人以為這是炒幣平台。關閉時前端一律送 leverage=1（等同現貨），
# 合約沒有任何改動，開回來只要設這一個變數。
# 只有 1 / true / on 算開，其餘（含未設定）都算關。
# VITE_SHOW_LEVERAGE=1

# ── Keeper config ─────────────────────────────────────────────────────────────
# Keeper config — set REAL values in GitHub Secrets (Settings > Secrets > Actions), NOT here.
# This file is a placeholder template. Never commit a real private key.

# Keeper Wallet Private Key (placeholder)
KEEPER_PRIVATE_KEY=0xYOUR_KEEPER_TEST_KEY

# RPC URL — Base Sepolia
KEEPER_RPC_URL=https://sepolia.base.org

# MockOracle Address on Base Sepolia
KEEPER_ORACLE_ADDRESS=0xeD90c4F3B48213888870C1FC8486921Cb0990Aa3
